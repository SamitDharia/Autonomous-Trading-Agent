name: Validate scripts

# Disabled: create_github_issues.ps1 script no longer exists in repository
# Re-enable this workflow if PowerShell scripts are added back to scripts/ folder

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  validate-powershell:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        shell: pwsh
        run: |
          Write-Host "No PowerShell scripts to validate - workflow disabled"
